import type defaultConfig from "./configType";
const config: defaultConfig = {
  headers: {
    display: true,
    width: "fixed",
  },
  footer: {
    display: true,
    width: "fixed",
  },
};

export default config;
